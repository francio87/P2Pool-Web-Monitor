services:
  p2pool-wm:
    image: ghcr.io/francio87/p2pool-web-monitor:dev
    healthcheck:
      test: ["CMD-SHELL", "test -s /output/data.json && test -s /output/history.json"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

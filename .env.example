# Optional advanced overrides. The default docker-compose.yml does not require this file.

P2POOL_DIR=/p2pool-data
DATA_API_DIR=/p2pool-data
OUTPUT=/output/index.html
HTTP_PORT=8080

WORKER_RECENTLY_OFFLINE_SECONDS=900
WORKER_RETENTION_SECONDS=86400
